{
    "version": 2,
    "builds": [{
        "src": "/",
        "use": "vercel-dart",
        "config": {
            "distDir": "."
        }
    }],
    "routes": [{
        "src": "/(.*)",
        "dest": "/"
    }],
    "scripts": {
        "/": "dart run bin/jualbeli_backend.dart"
    }
}